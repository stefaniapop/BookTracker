<%- include('./partials/top.ejs'); %>

<div class="container">
    <header>
        <%- include('./partials/navbar.ejs'); %>
    </header>
    <h2><%= carte.titlu %></h2>
    <img class="col-xs-6 col-md-3 col-lg-3 py-2 img-fluid" src="/uploads/books/<%= carte.coperta%>" alt="coperta-<%= carte.titlu %>" /></a>
    <h4><%= carte.nume_autor %></h4>
    <p><%= carte.descriere %></p>
    <p><%= carte.gen %></p>

    <% id_carte = carte.id_carte %>
    <% toRead = toRead %>
    <% read = read %>
    <% reading = reading %>
    <%- include('./partials/etichete.ejs', {id_carte: id_carte, toRead: toRead, read: read, reading: reading}); %>
    

    <div>
        <h3>Ai citit această carte?</h3>
            <form action="/review" method="POST">
                <div class="form-group">
                    <input type="text" class="d-none" name="id_carte" id="id_carte" value="<%= carte.id_carte %>">
                    </input>
                    <label for="review"> Spune ce crezi despre ea </label>
                    <textarea class="form-control" id="review" rows="3" name="review"></textarea>
                </div>
                <button type="submit" class="btn btn-outline-success mb-2">Trimite</button>
            </form>
    </div>
    <div>
    <% for (i=0; i < reviews.length; i++) {%>
        <h6><%= reviews[i].username %></h6>
       
        <h6><%= (reviews[i].data_review).slice(0,10) %></h6>
        <p><%= reviews[i].review_body %></p>
    <% } %>
    </div>
</div>

<%- include('./partials/bottom.ejs'); %>